{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @smartlogistica/shared build && pnpm --filter @smartlogistica/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @smartlogistica/web exec next start -p $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
